apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-deploy
  name: nginx-deploy
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30080
  selector:
    app: nginx-deploy
  sessionAffinity: None
  type: LoadBalancer
  externalIPs:
    - 165.232.183.103
